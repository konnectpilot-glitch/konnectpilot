{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install && cd artifacts/postpilot && PORT=25960 BASE_PATH=/ pnpm run build && cd ../api-server && pnpm run build"
  },
  "deploy": {
    "startCommand": "node artifacts/api-server/dist/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
